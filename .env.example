# Runtime
MODE=dry-run
LOG_DIR=logs
DISABLE_WEBSOCKET=true
MAX_TICKS=1
TICK_SECONDS=1.0
MARKET_SLUG=

# Underlying
BINANCE_SYMBOL=BTCUSDT

# Model / Decision
RV_FALLBACK=0.55
SIGMA_WEIGHT=1.0
IV=
EDGE_MIN=0.015
EDGE_BUFFER=0.010

# Risk
BANKROLL=10000
KELLY_FRACTION=0.25
F_CAP=0.05
MIN_ORDER_NOTIONAL=5

# Live mode auth (required for --mode live)
POLYMARKET_PRIVATE_KEY=
POLYMARKET_FUNDER=
POLYMARKET_SIGNATURE_TYPE=1
POLYMARKET_CHAIN_ID=137

# Optional API credentials (if omitted, the adapter creates/derives API creds)
POLYMARKET_API_KEY=
POLYMARKET_API_SECRET=
POLYMARKET_API_PASSPHRASE=
